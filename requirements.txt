httpx>=0.27
python-dotenv>=1.0
beautifulsoup4>=4.12
lxml>=5.0
pydantic>=2.0
fastapi>=0.100
uvicorn[standard]>=0.22

# Phase 2 vector index (Chroma / Pinecone + embeddings):
#   pip install -e ".[vector]"
